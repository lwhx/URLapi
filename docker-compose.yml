version: '3.8'

services:
  urlapi:
    image: ghcr.io/lwhx/urlapi:latest
    container_name: urlapi
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - GALLERY_PASSWORD=your_password_here
